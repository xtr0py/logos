services:
  logos:
    build: .
    container_name: logos
    restart: unless-stopped
    ports:
      - "8765:8000"
    volumes:
      - ./quotes.json:/data/quotes.json:ro
    environment:
      - QUOTES_PATH=/data/quotes.json
